class Solution:
    def isPalindrome(self, s):
        # Step 1: Filter and convert the string to lowercase
        filtered_str = ''.join(c.lower() for c in s if c.isalnum())
        
        # Step 2: Check if the filtered string is a palindrome
        return filtered_str == filtered_str[::-1]
